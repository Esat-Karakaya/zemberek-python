to run any of these files, you need to activate the virtual environment:
    source /home/esat/Desktop/zemberek-python/.venv/bin/activate

then run:
    python -m custom_tokenizer.<file name>
for example:
    python -m custom_tokenizer.detokenizer